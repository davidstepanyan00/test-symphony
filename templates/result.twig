<!DOCTYPE html>
<html>
<head>
    <title>Test</title>

    <style>
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .item {
            margin: 10px;
        }
    </style>
</head>
<body>

<div class="container">
      <div class="item">
        <p style="text-align: center; color:red">Wrongs Count: {{result.wrongsCount }}</p>
     <div>

     <div class="item">
        <ul>
            {% for wrongAnsweredQuestion in result.wrongAnsweredQuestions %}
                <li>
                    <h4 style="color:red">{{ wrongAnsweredQuestion.description }}</h4>
                </li>
            {% endfor %}
        </ul><br>
     </div>

         <div class="item">
             <p style="text-align: center; color:green">Rights Count: {{result.rightsCount }}</p>
       <div>

      <div class="item">
            <ul>
                {% for rightAnsweredQuestion in result.rightAnsweredQuestions %}
                    <li>
                        <h4 style="color:green">{{ rightAnsweredQuestion.description }}</h4>
                    </li>
                {% endfor %}
            </ul><br>
      </div>

         <div class="item">
                <a href="/"><button style="margin-left: 5px" >New Test</button></a>
         </div>
</div>
</body>
</html>